apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-monitor
  labels:
    app: app-monitor
spec:
  endpoints:
    - port: web
      scheme: http
      path: /metrics
      interval: 15s
  selector:
    matchLabels:
      app: app-monitor